# Become now uses Groq free inference with GPT-OSS 120B.
BECOME_MODEL=openai/gpt-oss-120b

# Optional server-side credential. If omitted, Become asks the user for a Groq key
# and keeps it only in the current browser tab's sessionStorage.
GROQ_API_KEY=
