# Become is internet-only: the browser never downloads or runs an AI model.
# Provision GROQ_API_KEY on the remote inference service for zero-friction live generation.
# If no server credential is configured, the browser can provide a session-only Groq key as an online fallback.
BECOME_MODEL=openai/gpt-oss-120b
GROQ_API_KEY=
